// By the way, you can use everything you just learned on streams. 
// ACTUALLY, you can use pretty much everything we've alread CREATED with streams because
// it was written in reusable fashion.

//Will use flyd as the stream lib since it's very friendly to ramda.